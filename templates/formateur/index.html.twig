{% extends 'base.html.twig' %}

{% block title %}Hello FormateurController!{% endblock %}

{% block body %}
<h3>Registre des formateurs</h3>
{% for formateur in formateurs %}
    <a class="btn btn-sm btn-outline-light" href="{{ path('show_formateur', {'id': formateur.id}) }}">{{ formateur }}</a> </br>
{% endfor %}
{% endblock %}
